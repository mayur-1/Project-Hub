Document for notesight-api┌───────────────────────────────┐ │        Developer Pushes       │ │       Code to Git Branch       │ │      (main / dev / prod)       │ └───────────────┬───────────────┘                 │                 ▼        ┌──────────────────┐        │ Jenkins Pipeline │        └──────────────────┘                 │                 ▼     ┌─────────────────────────────┐     │ Parameter: ENVIRONMENT      │     │ Choose DEV or PROD          │     └───────────────┬─────────────┘                     │                     ▼ ┌─────────────────────────────────────────┐ │ Stage 1: Create AWS Credentials          │ │ - Uses AWS CLI Docker image              │ │ - Gets ECR login password via AWS CLI    │ │ - Stores in variable AWS_REGISTRY_PASS   │ └─────────────────────────────────────────┘                     │                     ▼ ┌─────────────────────────────────────────┐ │ Stage 2: Add Cred Files                 │ │ - Fetch .env and Google creds from      │ │   Jenkins credentials store              │ │ - Save as .env and google-creds.json     │ └─────────────────────────────────────────┘                     │                     ▼ ┌─────────────────────────────────────────┐ │ Stage 3: Debug Paths                    │ │ - Print credential file paths            │ │ - Verify they exist via ls -lh           │ └─────────────────────────────────────────┘                     │                     ▼ ┌─────────────────────────────────────────┐ │ Stage 4: Build & Deploy (Docker Build)   │ │ - Build Docker image from Dockerfile     │ │ - Tag image as <branch>-<build#>         │ │ - Push to AWS ECR                        │ └─────────────────────────────────────────┘                     │                     ▼ ┌─────────────────────────────────────────┐ │ Stage 5: Create Remote Connection        │ │ - Create SSH connection object           │ │ - Uses Jenkins credential REMOTE_PROD_AWS│ │ - Target host: 3.16.210.41               │ └─────────────────────────────────────────┘                     │                     ▼ ┌─────────────────────────────────────────┐ │ Stage 6: Pull Docker Image Remotely      │ │ - SSH into EC2                           │ │ - Login to ECR using same credentials     │ │ - Pull the new Docker image              │ └─────────────────────────────────────────┘                     │                     ▼ ┌─────────────────────────────────────────┐ │ Stage 7: Execute Remote Commands         │ │ - Copy .env and google-creds.json        │ │ - Copy docker-compose file               │ │ - Create media/postgresql folders        │ │ - Run: docker stack deploy               │ │   → Deploys the container as a swarm     │ └─────────────────────────────────────────┘                     │                     ▼ ┌─────────────────────────────────────────┐ │ Post: Clean Workspace                   │ │ - Deletes Jenkins workspace files        │ │ - Keeps environment clean for next run   │ └─────────────────────────────────────────                     │                     ▼        ┌────────────────────────┐        │   Application Live     │        │   on AWS EC2 Server    │        │  (speech-to-text app)  │        └────────────────────────┘Pipeline Structure@Library('notesight-lib@1.0') _Loads a shared Jenkins library called notesight-lib (version 1.0).This library provides custom functions like:getEnv()buildAndDeploy()createRemoteConnection()pullDockerImage()sshPutFiles()sshExec()So the main Jenkinsfile uses pre-defined reusable code from this library.⚙️ pipeline { ... } → Main structureDefines all the pipeline configuration and stages. Agent and Optionsagent anyoptions {  disableConcurrentBuilds()  buildDiscarder(logRotator(numToKeepStr: '30'))  timeout(time: 1, unit: 'HOURS')  timestamps()  ansiColor('xterm')}✅ Explanationagent any: Run on any available Jenkins node/agent.disableConcurrentBuilds(): Prevent multiple builds running at the same time.buildDiscarder: Keep only the last 30 builds.timeout: Auto-fail if pipeline takes more than 1 hour.timestamps(): Adds timestamps to logs.ansiColor('xterm'): Enables colored console output.Parametersparameters {  choice(name: 'ENVIRONMENT', choices: ['', 'DEV', 'PROD'], description: 'Select Environment')}The user must select which environment to deploy:✅ DEV or PROD🌍 Environment Variablesenvironment {  APP_ENV = getEnv([    DEV: ['dev'],    PROD: ['main'],  ], ENVIRONMENT)  STACK_NAME="speech-to-text-${APP_ENV.toLowerCase()}"  AWS_REGISTRY_CREDENTIAL_USER = 'AWS'  AWS_REGISTRY_CREDENTIAL_PASS = 'generated-password'  DOCKER_REGISTRY = '026090536339.dkr.ecr.us-east-2.amazonaws.com/notesight-api'  IMAGE_TAG = "${GIT_BRANCH}-${BUILD_NUMBER}"  REMOTE = ''  REMOTE_WORKER = ''}✅ Explanation:APP_ENV: Converts the selected Jenkins ENVIRONMENT to a short name.DEV → "dev"PROD → "main"STACK_NAME: Used for Docker stack name, e.g., speech-to-text-devAWS_REGISTRY_CREDENTIAL_USER: Static label (used for login)AWS_REGISTRY_CREDENTIAL_PASS: Will be dynamically generated laterDOCKER_REGISTRY: AWS ECR registry URLIMAGE_TAG: Combines Git branch and Jenkins build numberREMOTE, REMOTE_WORKER: Placeholder for remote server connections Stage 1: Create AWS Credentialsstage('Create AWS Credentials') {  agent {    docker {      image 'amazon/aws-cli:2.17.30'      args '-v /var/run/docker.sock:/var/run/docker.sock --entrypoint=""'    }  }  environment {    AWS_REGION = 'us-east-2'    AWS_ACCESS_KEY_ID = 'AKIAQMEY57WJQTMOPNUK'    AWS_SECRET_ACCESS_KEY = credentials("AWS_SECRET_ACCESS_KEY")  }  steps {    script {      AWS_REGISTRY_CREDENTIAL_PASS = sh(script: "AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID} AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY} aws ecr get-login-password --region ${AWS_REGION}", returnStdout: true).trim()    }  }}✅ ExplanationRuns inside a Docker container with AWS CLI installed.Uses Jenkins credential AWS_SECRET_ACCESS_KEY.Uses AWS CLI command:aws ecr get-login-password→ Gets a temporary login password for ECR (Elastic Container Registry).Stores that password in variable AWS_REGISTRY_CREDENTIAL_PASS for later.🔐 Stage 2: Add Cred Filesstage('Add Cred Files') {  environment {    SECRET_ENV_FILE = credentials("ENV_${APP_ENV}")    SECRET_GOOGLE_CREDS_FILE = credentials("GOOGLE_CREDS_${APP_ENV}")  }  steps {    sh "cp ${SECRET_ENV_FILE} .env"    sh "cp ${SECRET_GOOGLE_CREDS_FILE} google-creds.json"  }}✅ ExplanationPulls credentials from Jenkins credentials store:.env file for app environment.google-creds.json file for Google Cloud access.Copies them into the workspace as:.envgoogle-creds.json🧩 Stage 3: Debug Pathsstage('Debug Paths') {  environment {    SECRET_ENV_FILE = credentials("ENV_${APP_ENV}")    SECRET_GOOGLE_CREDS_FILE = credentials("GOOGLE_CREDS_${APP_ENV}")  }  steps {    script {      def envPath = sh(script: "echo $SECRET_ENV_FILE", returnStdout: true).trim()      def googleCredsPath = sh(script: "echo $SECRET_GOOGLE_CREDS_FILE", returnStdout: true).trim()      sh "echo 'SECRET_ENV_FILE path: $envPath'"      sh "echo 'SECRET_GOOGLE_CREDS_FILE path: $googleCredsPath'"      sh "echo $envPath > /tmp/debug_secret_path"      sh "ls -lh $envPath"      sh "ls -lh $googleCredsPath"    }  }}✅ Purpose: DebuggingPrints and verifies that credential files exist and paths are correct before proceeding.🏗️ Stage 4: Build And Deploy (Docker)stage('Build And Deploy') {  steps {    script {      maskPasswords(varPasswordPairs: [[password: AWS_REGISTRY_CREDENTIAL_PASS]]) {        buildAndDeploy([          dockerRegistry: DOCKER_REGISTRY,          buildArgs: "-f .docker/ci/Dockerfile .",          registryCredentialUser: AWS_REGISTRY_CREDENTIAL_USER,          registryCredentialPass: AWS_REGISTRY_CREDENTIAL_PASS,          tags: [IMAGE_TAG],        ])      }    }  }}✅ ExplanationBuilds a Docker image using .docker/ci/DockerfileTags it with ${IMAGE_TAG}Pushes it to AWS ECR registry (DOCKER_REGISTRY)Credentials are masked in logs for securityUses buildAndDeploy() → function from notesight-lib🌐 Stage 5: Create Connection Config To Remote Serverstage('Create Connection Config To Remote Server') {  steps {    script {      REMOTE = createRemoteConnection(        APP_ENV,        [          [            condition: ['DEV', 'PROD'],            credentialsId: 'REMOTE_PROD_AWS',            host: '3.16.210.41',          ],        ]      )    }  }}✅ ExplanationCreates an SSH connection configuration to a remote EC2 server (3.16.210.41)Uses stored Jenkins SSH credential REMOTE_PROD_AWSReturns a REMOTE object used by later steps🐳 Stage 6: Pull Docker Api Image On Remote Serverstage('Pull Docker Api Image On Remote Server') {  steps {    script {      maskPasswords(varPasswordPairs: [[password: AWS_REGISTRY_CREDENTIAL_PASS]]) {        pullDockerImage([          remote: REMOTE,          dockerRegistry: DOCKER_REGISTRY,          registryCredentialUser: AWS_REGISTRY_CREDENTIAL_USER,          registryCredentialPass: AWS_REGISTRY_CREDENTIAL_PASS,          image: "$DOCKER_REGISTRY:$IMAGE_TAG"        ])      }    }  }}✅ ExplanationSSH into remote serverAuthenticate to AWS ECRPull the Docker image that was built and pushed in the previous stepUses pullDockerImage() helper from shared library⚙️ Stage 7: Execute Commands On Remote Serverstage('Execute Commands On Remote Server') {  environment {    PATH = mapping([        '/home/ubuntu/www/notesight-api-dev': ['DEV'],        '/home/ubuntu/www/notesight-api': ['PROD'],    ], APP_ENV)    COMPOSE_FILE = mapping([        'docker-compose-dev.yml': ['DEV'],        'docker-compose-prod.yml': ['PROD'],    ], APP_ENV)  }  steps {    sshPutFiles(REMOTE, PATH, ['.env*', 'google-creds.json', 'docker-compose*.yml']);    sshExec remote: REMOTE, path: PATH, env: [TAG: IMAGE_TAG], command: """      cat $COMPOSE_FILE | docker-compose -f - config > docker-compose.yml      mkdir -p media postgresql      docker stack deploy --with-registry-auth -c docker-compose.yml $STACK_NAME    """  }}✅ ExplanationUses mapping() to decide remote paths and files based on environment.DEV → /home/ubuntu/www/notesight-api-devPROD → /home/ubuntu/www/notesight-apiCopies environment and compose files to remote server (sshPutFiles).Then via SSH:Generates a full compose file.Creates media and postgresql folders.Deploys Docker stack using:docker stack deploy --with-registry-auth -c docker-compose.yml $STACK_NAMESo it runs the app as a Docker Swarm service.Post Sectionpost {  always {    script {      cleanWs()    }  }}✅ Explanation: Cleans Jenkins workspace after every build (success or fail).