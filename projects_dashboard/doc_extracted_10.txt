Notsight API✅ Important (Based on Our Current Architecture)Right now, our setup is:One EC2 nodeDocker Swarm (single-node swarm)API, Migration, Redis running inside the same EC2Jenkins deploys → Docker Stack deployNo load balancerNo auto scalingRedis, Mongo, Kafka not external👉 This architecture cannot auto-scale.Because:ComponentProblemDocker SwarmRequires multi-node cluster to scale replicas across nodesRedis inside EC2Cannot scale or share between machinesMigration inside containerRuns on every deploy, unsafe for multi-nodeEC2 not behind ALBCannot load balance multiple EC2sASG cannot work because internal IP changes will break your setupSo ASG + ALB will not work until we externalize our dependencies.⭐ What Need to Change to Enable Auto ScalingSTEP 1 — Move Redis → AWS ElastiCacheBecause:Redis inside EC2 cannot be used by multiple EC2 nodes.ElastiCache is cluster-safe.Your API container must connect using:Ex:REDIS_HOST = your-cluster-endpoint.amazonaws.comREDIS_PORT = 6379STEP 2 — Move Kafka → AWS MSKKafka inside EC2 cannot be used in auto-scaling environment.We need:KAFKA_BROKER_URL = b-1.msk-cluster.amazonaws.com:9092STEP 3 — Move PostgreSQLWhere is it currently?✔ If it's on RDS → OK❌ If it's inside EC2 → Need to migrate to RDSSTEP 4 — Remove Redis / Mongo / Kafka FROM docker-composeOur final docker-compose-prod.yml must only include:✔ API✔ Migrations (maybe)❌ Redis❌ Mongo❌ KafkaSTEP 5 — You need to stop using Docker SwarmAuto Scaling cannot work with Docker Swarm unless we build a multi-node cluster and manage join tokens yourself.✔ Simpler option:Run only Docker Compose on each EC2 instance.✔ Best option (recommended):Use ECS Fargate. No need for EC2, auto scaling is automatic.But if you want to continue with EC2 + Docker Compose:Every ASG instance will run docker compose on bootJenkins will just push image to ECREC2 will pull & restart container when ALB health checks failSTEP 6 — Create AMI for ASGWe need a clean EC2 with:Docker installeddocker-compose installed/home/ubuntu/www/notesight-api folderStart script like:docker-compose pulldocker-compose up -dThen:✔ Create AMI✔ Create Launch Template✔ Create ASG✔ Attach ALB Target Group✔ Set scaling policiesSTEP 7 — Update Jenkins Pipelinewe must remove all SSH steps because code will not deploy to a single EC2 anymore.Instead:New Jenkins Flow:1. Build Docker Image  2. Push to ECR  3. Update tag in SSM Parameter Store  4. EC2 user-data script pulls latest image  5. ALB health-check ensures no downtimeUpdated docker-compose-prod.yml (after externalizing services)version: '3.5'services:  api:    image: 026090536339.dkr.ecr.us-east-2.amazonaws.com/notesight-api:${TAG}    restart: always    env_file: .env    ports:      - $PORT:$PORT    command: yarn start:prod**Even though Kafka and postgress are NOT in our docker-composestill cannot Auto-Scale unless they are EXTERNAL.**Why? → Because your application still connects to internal EC2 Mongo/Kafka via .env.ASG-Ready DeploymentThis document contains everything you requested:ASG-ready Jenkinsfile (Groovy) — builds, pushes to ECR, updates SSM.docker-compose-prod.yml — minimal, stateless api service.EC2 user-data script — boots, installs docker, pulls tag from SSM, runs compose.Step-by-step ASG + ALB creation guide — Terraform or AWS Console steps, health checks, scaling policies.Architecture diagram (ASCII) + notes.1) Jenkinsfile (ASG-ready)@Library('notesight-lib@1.0') _pipeline {  agent any  options {    disableConcurrentBuilds()    buildDiscarder(logRotator(numToKeepStr: '30'))    timeout(time: 1, unit: 'HOURS')    timestamps()    ansiColor('xterm')  }  parameters {    choice(name: 'ENVIRONMENT', choices: ['', 'DEV', 'PROD'], description: 'Select Environment')  }  environment {    APP_ENV = getEnv([DEV: ['dev'], PROD: ['main']], ENVIRONMENT)    DOCKER_REGISTRY = '026090536339.dkr.ecr.us-east-2.amazonaws.com/notesight-api'    IMAGE_TAG = "${GIT_BRANCH}-${BUILD_NUMBER}"    AWS_REGION = 'us-east-2'  }  stages {    stage('Prepare') {      steps {        script {          echo "Building for ${APP_ENV} with tag ${IMAGE_TAG}"        }      }    }    stage('Build Image') {      steps {        sh 'docker build -t ${DOCKER_REGISTRY}:${IMAGE_TAG} -f .docker/ci/Dockerfile .'      }    }    stage('Login to ECR & Push') {      steps {        withCredentials([usernamePassword(credentialsId: 'AWS_ECR', usernameVariable: 'AWS_USER', passwordVariable: 'AWS_PASS')]) {          sh '''            aws ecr get-login-password --region ${AWS_REGION} | docker login --username AWS --password-stdin ${DOCKER_REGISTRY}            docker push ${DOCKER_REGISTRY}:${IMAGE_TAG}          '''        }      }    }    stage('Run DB Migrations (one-off)') {      when { expression { return params.ENVIRONMENT == 'PROD' || params.ENVIRONMENT == 'DEV' } }      steps {        echo 'Running migrations from Jenkins (one-off)'        sh "docker run --rm ${DOCKER_REGISTRY}:${IMAGE_TAG} yarn db:up"      }    }    stage('Update Image Tag in SSM') {      steps {        sh '''          aws ssm put-parameter --name "/notesight/${APP_ENV}/api/TAG" --type "String" --overwrite --value "${IMAGE_TAG}" --region ${AWS_REGION}        '''      }    }    stage('Notify') {      steps {        echo 'Deployment artifact published and SSM updated.'      }    }  }  post {    always { cleanWs() }  }}Notes:Replace AWS_ECR with your Jenkins credential id for ECR (or use IAM role on Jenkins host).docker run --rm ... yarn db:up executes migrations once from Jenkins — avoids running migrations on every ASG instance.You can restrict the migration step to only run on main / prod branches.2) docker-compose-prod.yml (minimal, stateless)version: '3.5'services:  api:    image: 026090536339.dkr.ecr.us-east-2.amazonaws.com/notesight-api:${TAG}    restart: always    env_file: .env    ports:      - "$PORT:$PORT"    command: yarn start:prod# Note: Redis, Kafka, Postgres must be external services (ElastiCache, MSK, RDS)Notes:Keep .env only with external hostnames (RDS endpoint, ElastiCache endpoint, MSK brokers).Do not include migration or redis service here.3) EC2 user-data script (for Launch Template)Save as user-data.sh in launch template (bash).#!/bin/bashset -e# Install updates and dockeryum update -yamazon-linux-extras install docker -y || truesystemctl enable --now dockerusermod -a -G docker ec2-user# Install docker compose pluginmkdir -p /usr/local/lib/docker/cli-pluginscurl -SL "https://github.com/docker/compose/releases/download/v2.11.2/docker-compose-linux-x86_64" -o /usr/local/lib/docker/cli-plugins/docker-composechmod +x /usr/local/lib/docker/cli-plugins/docker-compose# Configure AWS CLI (instance role recommended; otherwise configure credentials securely)AWS_REGION="us-east-2"APP_ENV="prod"SSM_PARAM="/notesight/${APP_ENV}/api/TAG"DEPLOY_DIR="/home/ec2-user/notesight-api"mkdir -p ${DEPLOY_DIR}cd ${DEPLOY_DIR}# Wait until network is readysleep 10# Fetch TAG from SSM (with retries)for i in {1..10}; do  TAG=$(aws ssm get-parameter --name "${SSM_PARAM}" --query "Parameter.Value" --output text --region ${AWS_REGION} 2>/dev/null || true)  if [ -n "$TAG" ]; then break; fi  sleep 6doneif [ -z "$TAG" ]; then  echo "ERROR: failed to fetch TAG from SSM"  exit 1fi# Fetch .env from SSM (or S3). Example: SSM string parameter '/notesight/prod/env' with newline separated KEY=VALUEENV_PARAM="/notesight/${APP_ENV}/env"aws ssm get-parameter --name "$ENV_PARAM" --with-decryption --query "Parameter.Value" --output text --region ${AWS_REGION} > .env || echo "Warning: .env not fetched"# Pull and startaws ecr get-login-password --region ${AWS_REGION} | docker login --username AWS --password-stdin 026090536339.dkr.ecr.us-east-2.amazonaws.comdocker pull 026090536339.dkr.ecr.us-east-2.amazonaws.com/notesight-api:$TAG || exit 2# create docker-compose file (if not present)cat > docker-compose.yml <<'EOF'version: '3.5'services:  api:    image: 026090536339.dkr.ecr.us-east-2.amazonaws.com/notesight-api:${TAG}    restart: always    env_file: .env    ports:      - "$PORT:$PORT"    command: yarn start:prodEOF# Export TAG for composeexport TAG=$TAG# Start/usr/local/lib/docker/cli-plugins/docker-compose up -d# Optional: setup a cron or systemd timer to check for new TAG every 60s (or use lifecycle hooks / CodeDeploy)Notes:Use an Instance Profile (IAM role) for EC2 with permissions: ssm:GetParameter, ecr:GetAuthorizationToken, ecr:BatchGetImage, ecr:GetDownloadUrlForLayer, s3:GetObject (if you store env in S3).You can replace SSM .env with S3 object for larger env files.4) ASG + ALB creation guide (Console steps)A. Prepare AMI / Launch TemplatePrepare a golden EC2 (same OS, docker installed) and configure /home/ec2-user/notesight-api directory.Create an AMI from this base instance.Create a Launch Template using that AMI and set the user-data to the user-data.sh. Configure the Instance Profile (IAM role) with necessary permissions.B. Create Target GroupGo to EC2 > Target Groups > Create target groupTarget type: instanceProtocol: HTTPPort: 80 (or your app port)Health checks: Path /health (or your app health endpoint)Healthy threshold: 2, Unhealthy: 2, Interval: 10sC. Create ALBEC2 > Load Balancers > Create Application Load BalancerScheme: internet-facing (or internal)Add listeners: HTTP 80 (or HTTPS with cert)Attach Target Group created aboveD. Create Auto Scaling GroupEC2 > Auto Scaling Groups > Create Auto Scaling GroupUse Launch TemplateChoose subnetsAttach to the Target GroupSet desired/min/max instances (e.g., min=2, desired=2, max=6)E. Scaling Policies (example)Target Tracking — Average CPU Utilization = 60%Step Scaling — scale out +1 at CPU >= 70% for 2 periodsF. Rolling deployments (zero downtime)Update SSM parameter with new TAG (Jenkins does this).Use lifecycle hooks or run a small script on instances to watch SSM and gracefully pull new image then docker compose up -d.Alternatively use CodeDeploy + ASG deployments for automated in-place/blue-green updates.5) Architecture Diagram            internet               |             ALB (HTTP/HTTPS)               |        ---------------------        |        |         |     EC2 #1   EC2 #2    EC2 #N   <- ASG (Docker + docker-compose pulling same image)   (docker)  (docker)   (docker)      |         |          |      ----------------------             VPC Private      |        |        |  ElastiCache  MSK    RDS(Postgres)     (Redis)   (Kafka)   (Primary)Notes:All EC2 instances use the same .env (stored encrypted in SSM or S3).Migrations are run once from Jenkins (not on instance boot).6) Health checks & readinessImplement /health endpoint that verifies:App process upCan reach RedisCan reach Postgres(Optionally) can reach KafkaALB should use this path.7) Optional: Blue/Green via Target GroupsCreate two target groups (tg-blue, tg-green) and two ASGs or use CodeDeploy to swap traffic.8) Security & IAMIAM Role for EC2 (Instance Profile): AmazonEC2ContainerRegistryReadOnly, AmazonSSMReadOnlyAccess (or custom with ssm:GetParameter), logs:CreateLogStream if you push logs.Secrets should be stored in SSM Parameter Store (SecureString) or Secrets Manager; do NOT keep secrets in plain .env in AMI.9) Troubleshooting checklistIf new instance fails health check: docker compose logs api and journalctl -u docker.If TAG not pulled: verify ssm:GetParameter permission and SSM parameter name.If docker login fails: ensure instance role has ECR permissions OR store creds securely.